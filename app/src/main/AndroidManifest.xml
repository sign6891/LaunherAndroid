<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.launcherandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:launchMode="singleTask"
        android:roundIcon="@mipmap/ic_launcher_round"
        tools:ignore="AllowBackup">
        <activity android:name=".addapplicationactivity.AddApplicationActivity"
            android:theme="@style/AppTheme"
            android:label="@string/add_application">
        </activity>
        <activity
            android:name=".allapplicationactivity.AppListActivity"
            android:label="@string/all_installed_applications"
            android:theme="@style/AppTheme">

        </activity>
        <activity
            android:name=".homeactivity.MainActivity"
            android:label="@string/featured_apps"
            android:stateNotNeeded="true"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="RtlEnabled">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>